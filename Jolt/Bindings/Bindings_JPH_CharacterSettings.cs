namespace Jolt
{
    internal static unsafe partial class Bindings
    {
        public static void JPH_CharacterSettings_Init(NativeHandle<JPH_CharacterSettings> settings)
        {
            UnsafeBindings.JPH_CharacterSettings_Init(settings);
        }
    }
}
